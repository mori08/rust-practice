# rust-practice

Rust の学習・練習用リポジトリ。単一のバイナリクレート(edition 2024)。

学習の進捗(習得済み/取り組み中のトピック)は `notes/progress.md` を参照。解説の深さはこれに合わせる。

## コマンド

- ビルド・実行: `cargo run`
- テスト: `cargo test`
- リント: `cargo clippy`
- フォーマット: `cargo fmt`(コミット前に `cargo fmt --check` と `cargo clippy` を通すこと)

## 方針

- このリポジトリは Rust 学習用。**コードは基本的にユーザー本人が書く**
- Claude の役割はコードを読んでのアドバイス・解説・レビューが中心。
  - 求められない限りコードを代わりに書かない
- 解説では「なぜそうなるか」(所有権・借用・トレイトなどの背景)まで踏み込む
- より良い書き方や慣用的な書き方(イディオム)があれば提案する
- ツールチェーンは `rust-toolchain.toml` で stable に固定している
